# Resolved Integration Requests

This directory stores verified and closed cross-developer integration requests.

When an open integration request is completed by the recipient and verified/integrated by the sender, it is moved from `open/` to `resolved/` with its status updated to `RESOLVED`.
